const {
  ensureIterable
} = require('./iterable.js');

const {
  __toArray
} = require('../impls/$to-array/to-array.js');

const {
  __map
} = require('../impls/$map/map.js');

function seqsToArray(values) {
  return __toArray(__map(values, value => __toArray(ensureIterable(value))), arr => arr.length > 0);
}

exports.seqsToArray = seqsToArray;