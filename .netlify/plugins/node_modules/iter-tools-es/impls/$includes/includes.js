const {
  iterableCurry
} = require('../../internal/iterable.js');

const {
  __findOr
} = require('../$find-or/find-or.js');

const none = Symbol('none');

function __includes(iterable, value, same = Object.is) {
  return __findOr(iterable, none, v => same(value, v)) !== none;
}

exports.__includes = __includes;
const includes = /*#__PURE__*/iterableCurry(__includes, {
  minArgs: 1,
  maxArgs: 2,
  reduces: true,

  validateArgs(args) {
    if (true && typeof args[0] === 'string') {
      console.warn(`For string inputs use includesSeq instead of includes`);
    }
  }

});
exports.includes = includes;