const {
  asyncIterableCurry
} = require('../../internal/async-iterable.js');

const {
  __asyncToArray
} = require('../$to-array/async-to-array.js');

async function* __asyncReverse(source) {
  yield* (await __asyncToArray(source)).reverse();
}

exports.__asyncReverse = __asyncReverse;
const asyncReverse = /*#__PURE__*/asyncIterableCurry(__asyncReverse);
exports.asyncReverse = asyncReverse;