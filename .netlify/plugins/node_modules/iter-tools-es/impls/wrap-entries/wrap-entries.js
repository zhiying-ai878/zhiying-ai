const {
  wrapWithIterableIterator
} = require('../../internal/iterable.js');

function* __wrapEntries(entriesable) {
  if (entriesable != null) {
    yield* entriesable.entries();
  }
}

exports.__wrapEntries = __wrapEntries;
const wrapEntries = /*#__PURE__*/wrapWithIterableIterator(__wrapEntries);
exports.wrapEntries = wrapEntries;