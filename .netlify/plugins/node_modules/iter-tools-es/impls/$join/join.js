const {
  wrapWithIterableIterator,
  ensureIterable
} = require('../../internal/iterable.js');

const {
  __joinWithSeq
} = require('../$join-with-seq/join-with-seq.js');

const emptySeq = [];

function __join(source) {
  return __joinWithSeq(ensureIterable(source), emptySeq);
}

exports.__join = __join;
const join = /*#__PURE__*/wrapWithIterableIterator(__join);
exports.join = join;