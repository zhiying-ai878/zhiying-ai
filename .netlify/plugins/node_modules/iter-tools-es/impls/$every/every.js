const {
  iterableCurry
} = require('../../internal/iterable.js');

function __every(iterable, predicate) {
  let c = 0;

  for (const value of iterable) {
    if (!predicate(value, c++)) {
      return false;
    }
  }

  return true;
}

exports.__every = __every;
const every = /*#__PURE__*/iterableCurry(__every, {
  reduces: true
});
exports.every = every;