const {
  asyncIterableCurry
} = require('../../internal/async-iterable.js');

async function __asyncStr(chars) {
  let result = '';

  for await (const char of chars) {
    result += char.toString();
  }

  return result;
}

exports.__asyncStr = __asyncStr;
const asyncStr = /*#__PURE__*/asyncIterableCurry(__asyncStr, {
  reduces: true
});
exports.asyncStr = asyncStr;