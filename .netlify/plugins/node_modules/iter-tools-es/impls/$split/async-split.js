const {
  asyncIterableCurry
} = require('../../internal/async-iterable.js');

const {
  __asyncMap
} = require('../$map/async-map.js');

function* iterableOf(value) {
  yield value;
}

function __asyncSplit(source) {
  return __asyncMap(source, value => iterableOf(value));
}

exports.__asyncSplit = __asyncSplit;
const asyncSplit = /*#__PURE__*/asyncIterableCurry(__asyncSplit);
exports.asyncSplit = asyncSplit;