const {
  asyncIterableCurry
} = require('../../internal/async-iterable.js');

async function* __asyncTake(iterable, n) {
  if (n === 0) return;
  let i = 0;

  for await (const value of iterable) {
    yield value;
    if (++i === n) break;
  }
}

exports.__asyncTake = __asyncTake;
const asyncTake = /*#__PURE__*/asyncIterableCurry(__asyncTake);
exports.asyncTake = asyncTake;