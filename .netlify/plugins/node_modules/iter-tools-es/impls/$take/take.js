const {
  iterableCurry
} = require('../../internal/iterable.js');

function* __take(iterable, n) {
  if (n === 0) return;
  let i = 0;

  for (const value of iterable) {
    yield value;
    if (++i === n) break;
  }
}

exports.__take = __take;
const take = /*#__PURE__*/iterableCurry(__take);
exports.take = take;