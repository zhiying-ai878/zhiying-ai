const {
  iterableCurry
} = require('../../internal/iterable.js');

function* __drop(iterable, n) {
  let i = 0;

  for (const value of iterable) {
    if (i++ >= n) yield value;
  }
}

exports.__drop = __drop;
const drop = /*#__PURE__*/iterableCurry(__drop);
exports.drop = drop;