const {
  asyncIterableCurry
} = require('../../internal/async-iterable.js');

async function* __asyncDrop(iterable, n) {
  let i = 0;

  for await (const value of iterable) {
    if (i++ >= n) yield value;
  }
}

exports.__asyncDrop = __asyncDrop;
const asyncDrop = /*#__PURE__*/asyncIterableCurry(__asyncDrop);
exports.asyncDrop = asyncDrop;