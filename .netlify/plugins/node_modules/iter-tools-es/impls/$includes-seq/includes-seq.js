const {
  iterableCurry,
  ensureIterable
} = require('../../internal/iterable.js');

const {
  __includesAnySeq
} = require('../$includes-any-seq/includes-any-seq.js');

function __includesSeq(iterable, seq, same = Object.is) {
  return __includesAnySeq(iterable, [seq], same);
}

exports.__includesSeq = __includesSeq;
const includesSeq = /*#__PURE__*/iterableCurry(__includesSeq, {
  minArgs: 1,
  maxArgs: 2,
  reduces: true,

  validateArgs(args) {
    args[1] = ensureIterable(args[1]);
  }

});
exports.includesSeq = includesSeq;