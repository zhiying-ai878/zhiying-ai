const {
  iterableCurry,
  cache
} = require('../../internal/iterable.js');

function* __interposeSeq(source, seq) {
  const subseq_ = cache(seq);
  let first = true;

  for (const sourceValue of source) {
    if (!first) yield* subseq_;
    yield sourceValue;
    first = false;
  }
}

exports.__interposeSeq = __interposeSeq;
const interposeSeq = /*#__PURE__*/iterableCurry(__interposeSeq);
exports.interposeSeq = interposeSeq;