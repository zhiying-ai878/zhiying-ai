const {
  asyncIterableCurry
} = require('../../internal/async-iterable.js');

async function* __asyncTap(source, callback) {
  let c = 0;

  for await (const value of source) {
    callback(value, c++);
    yield value;
  }
}

exports.__asyncTap = __asyncTap;
const asyncTap = /*#__PURE__*/asyncIterableCurry(__asyncTap);
exports.asyncTap = asyncTap;