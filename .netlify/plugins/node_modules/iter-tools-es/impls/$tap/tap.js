const {
  iterableCurry
} = require('../../internal/iterable.js');

function* __tap(source, callback) {
  let c = 0;

  for (const value of source) {
    callback(value, c++);
    yield value;
  }
}

exports.__tap = __tap;
const tap = /*#__PURE__*/iterableCurry(__tap);
exports.tap = tap;