const {
  iterableCurry
} = require('../../internal/iterable.js');

const {
  __toArray
} = require('../$to-array/to-array.js');

function* __joinWithSeq(source, separatorSeq) {
  const _separatorSeq = __toArray(separatorSeq);

  let isFirst = true;

  for (const value of source) {
    if (!isFirst) yield* _separatorSeq;
    yield* value;
    isFirst = false;
  }
}

exports.__joinWithSeq = __joinWithSeq;
const joinWithSeq = /*#__PURE__*/iterableCurry(__joinWithSeq);
exports.joinWithSeq = joinWithSeq;