const {
  asyncEnsureIterable
} = require('../../internal/async-iterable.js');

async function __asyncSize(iterable) {
  const iter = asyncEnsureIterable(iterable);
  let size = 0;

  for await (const _ of iter) {
    size++;
  }

  return size;
}

exports.__asyncSize = __asyncSize;
const asyncSize = __asyncSize;
exports.asyncSize = asyncSize;