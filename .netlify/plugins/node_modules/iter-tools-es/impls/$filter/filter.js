const {
  iterableCurry
} = require('../../internal/iterable.js');

function* __filter(source, predicate) {
  let c = 0;

  for (const value of source) {
    if (predicate(value, c++)) {
      yield value;
    }
  }
}

exports.__filter = __filter;
const filter = /*#__PURE__*/iterableCurry(__filter);
exports.filter = filter;