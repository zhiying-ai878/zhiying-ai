const {
  iterableCurry
} = require('../../internal/iterable.js');

function __forEach(iterable, callback) {
  let c = 0;

  for (const value of iterable) {
    callback(value, c++);
  }
}

exports.__forEach = __forEach;
const forEach = /*#__PURE__*/iterableCurry(__forEach, {
  reduces: true
});
exports.forEach = forEach;