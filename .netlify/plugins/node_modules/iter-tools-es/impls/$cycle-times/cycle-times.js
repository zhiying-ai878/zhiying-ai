const {
  iterableCurry
} = require('../../internal/iterable.js');

const {
  __toArray
} = require('../$to-array/to-array.js');

function* __cycleTimes(source, n) {
  const arr = Array.isArray(source) ? source : __toArray(source);
  if (!arr.length) return;

  while (n--) {
    yield* arr;
  }
}

exports.__cycleTimes = __cycleTimes;
const cycleTimes = /*#__PURE__*/iterableCurry(__cycleTimes);
exports.cycleTimes = cycleTimes;