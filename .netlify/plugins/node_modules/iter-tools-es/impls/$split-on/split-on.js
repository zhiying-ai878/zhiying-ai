const {
  iterableCurry
} = require('../../internal/iterable.js');

const {
  __splitWhen
} = require('../$split-when/split-when.js');

function __splitOn(source, separator, same = Object.is) {
  return __splitWhen(source, value => same(separator, value));
}

exports.__splitOn = __splitOn;
const splitOn = /*#__PURE__*/iterableCurry(__splitOn, {
  minArgs: 1,
  maxArgs: 2,

  validateArgs(args) {
    if (true && typeof args[0] === 'string') {
      console.warn(`For string inputs use splitOnSeq instead of splitOn`);
    }
  }

});
exports.splitOn = splitOn;