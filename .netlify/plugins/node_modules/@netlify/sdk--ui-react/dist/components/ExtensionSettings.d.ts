import { default as React } from 'react';

export type ExtensionSettingsProps = {
    children: React.ReactNode;
    subheader?: React.ReactNode | ((options: {
        name: string;
    }) => React.JSX.Element) | undefined;
};
export declare const ExtensionSettings: ({ children, subheader, }: ExtensionSettingsProps) => React.JSX.Element;
//# sourceMappingURL=ExtensionSettings.d.ts.map