import { IconName } from '@netlify/ui';
import { AlertMessage, AlertSharedProps, AlertState } from './types';

export declare const parseProps: ({ error, warning, info, success, upgrade, role, iconName, }: Partial<AlertSharedProps>) => {
    message: AlertMessage;
    role: string;
    state: AlertState;
    icon: IconName;
};
//# sourceMappingURL=helpers.d.ts.map