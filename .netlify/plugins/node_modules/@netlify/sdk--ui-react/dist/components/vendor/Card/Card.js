import { jsx } from "react/jsx-runtime";
import { cn } from "@netlify/ui";
import '../../../assets/Card.css';const Card = ({ variant, children, className }) => /* @__PURE__ */ jsx(
  "section",
  {
    className: cn(
      // todo(calebbarnes): theme not working in css modules
      // styles["card"],
      // typeof variant === "string" && styles[`card-${variant}`],
      "card",
      variant && `card-${variant}`,
      className
    ),
    children
  }
);
export {
  Card
};
//# sourceMappingURL=Card.js.map
