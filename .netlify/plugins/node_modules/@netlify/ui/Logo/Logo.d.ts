import { LogoSize } from './helpers';
export interface LogoProps {
    /** The name of the Team/Organization that the logo is assigned to */
    name?: string;
    /** The id of the Team/Organization that the logo is assigned to */
    teamId?: string;
    /** The width and height of the logo */
    size?: LogoSize;
    /** The absolute path to the logo */
    src?: string;
    /** Class name to pass to the outermost element */
    className?: string;
    /** Class name to pass to the element rendering the image or placeholder */
    elementClassName?: string;
    /** Determines the text to show if no logo is provided */
    placeholderName?: string;
}
export declare const Logo: ({ name, teamId, size, src, className, elementClassName, placeholderName, }: LogoProps) => import("react/jsx-runtime").JSX.Element;
