import { humanizePlanName, normalizeTeamPlanName, isPlanEnterpriseTrial } from "./plans.js";
const getAccountNormalizedPlanName = (account) => {
  const planName = account == null ? void 0 : account.type_name;
  const normalizedPlanName = normalizeTeamPlanName(planName);
  const isTrial = isPlanEnterpriseTrial(account == null ? void 0 : account.type_slug);
  return `${normalizedPlanName}${isTrial ? " (free month)" : ""}`;
};
const getAccountPlanTitle = (account) => humanizePlanName(getAccountNormalizedPlanName(account));
export {
  getAccountNormalizedPlanName,
  getAccountPlanTitle
};
//# sourceMappingURL=account.js.map
