export declare const isExternalTeamsOrganization: (organization?: {
    id: string;
    name: string;
} | undefined) => boolean;
export declare const createExternalTeamsOrganization: <Account extends {
    id: string;
}>(accounts: Account[]) => {
    id: string;
    name: string;
    accounts: Account[];
    logo_url: string;
};
