"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const helpers_plans = require("./plans.cjs");
const getAccountNormalizedPlanName = (account) => {
  const planName = account == null ? void 0 : account.type_name;
  const normalizedPlanName = helpers_plans.normalizeTeamPlanName(planName);
  const isTrial = helpers_plans.isPlanEnterpriseTrial(account == null ? void 0 : account.type_slug);
  return `${normalizedPlanName}${isTrial ? " (free month)" : ""}`;
};
const getAccountPlanTitle = (account) => helpers_plans.humanizePlanName(getAccountNormalizedPlanName(account));
exports.getAccountNormalizedPlanName = getAccountNormalizedPlanName;
exports.getAccountPlanTitle = getAccountPlanTitle;
//# sourceMappingURL=account.cjs.map
