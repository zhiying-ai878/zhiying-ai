import { AvatarUser } from '../Avatar/Avatar';
import { IDropdownItem } from '../Dropdown/Dropdown';
interface UserDropdownProps {
    user?: AvatarUser;
    className?: string;
    onLogout?: () => void;
    alignRight?: boolean;
    userPath: string;
    links?: IDropdownItem[];
    showUserDetails?: boolean;
    menuPosition?: 'above' | 'below';
    isNavCollapsed?: true | undefined;
}
export declare function UserDropdown({ user, className, onLogout, userPath, alignRight, links, showUserDetails, menuPosition, isNavCollapsed, }: UserDropdownProps): import("react/jsx-runtime").JSX.Element | null;
export {};
