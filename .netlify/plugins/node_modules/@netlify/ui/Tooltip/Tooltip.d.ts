import { TippyProps } from '@tippyjs/react';
import { IconName } from '../Icon/types';
import { TooltipPluginProps } from './TippyPlugins';
export interface TooltipProps extends Omit<TippyProps & TooltipPluginProps, 'content'> {
    description: string | TippyProps['content'];
    floating?: boolean;
    nowrap?: boolean;
    outset?: string;
    intent?: 'info' | 'warning' | 'error';
    iconName?: IconName;
    buttonClassName?: string;
    title?: string;
    onClick?: () => void;
    id?: string;
}
export declare const Tooltip: ({ floating, nowrap, outset, children, intent, iconName, buttonClassName, title, appendTo, description, maxWidth, offset, visible, className, onClickOutside, onClick, id, manageFocusInPopover, ...rest }: TooltipProps) => import("react/jsx-runtime").JSX.Element;
