import { ImageFormat } from './helpers';
export interface PictureProps {
    alt: string;
    url: string;
    width: number;
    height: number;
    fallbackFormat?: ImageFormat;
    /** Supplied in priority order */
    formats?: ImageFormat[];
    className?: string;
    onError?: (e: any) => void;
    animated?: boolean;
    cover?: boolean;
    title?: string;
}
export declare const Picture: ({ alt, url, width, height, formats, fallbackFormat, className, onError, animated, cover, title, }: PictureProps) => import("react/jsx-runtime").JSX.Element;
