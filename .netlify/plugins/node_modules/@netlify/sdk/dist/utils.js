import chalk from "chalk";
export const ntliLog = (message, name = `Netlify SDK`) => {
    console.log(`${chalk.blue(name)} ${message}\n`);
};
export const printResetHeader = () => {
    // leave a marker line for last output
    console.log(chalk.bold.yellow(`─`.repeat(process.stdout.columns || 4)));
    // push all terminal output out of view
    console.log(`\n`.repeat(process.stdout.rows || 4));
};
export const envVarFromSlug = (slug) => {
    const safeSlug = slug.toUpperCase().replaceAll(/[^A-Z_]/g, "_");
    if (safeSlug.startsWith("_")) {
        return `I${safeSlug}`;
    }
    return safeSlug;
};
export const parseNonUndefined = (value) => {
    if (value === undefined) {
        throw new TypeError("Expected value to not be undefined but received: undefined");
    }
    return value;
};
//# sourceMappingURL=utils.js.map