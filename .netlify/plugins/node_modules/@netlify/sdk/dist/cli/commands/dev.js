import process from "node:process";
import { build } from "./build.js";
export const dev = async (ctx, { ...opts }) => {
    process.env.NODE_ENV = `development`;
    await build(ctx, { ...opts, mode: "dev" });
};
//# sourceMappingURL=dev.js.map