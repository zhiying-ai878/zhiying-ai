import { type BaseMessage } from "./schemas.js";
export { type BaseMessage };
export interface MessageReader<Message extends BaseMessage = BaseMessage> {
    subscribe(options?: {
        signal?: AbortSignal | undefined;
    }): AsyncIterable<Message>;
}
export interface MessageWriter<Message extends BaseMessage = BaseMessage> {
    send(message: Message): Promise<void>;
}
/**
 * MessageChannel is a two-way messaging channel intended to facilitates communication across an
 * iframe.
 */
export interface MessageChannel<ReadMessage extends BaseMessage = BaseMessage, WriteMessage extends BaseMessage = BaseMessage> extends MessageReader<ReadMessage>, MessageWriter<WriteMessage> {
}
//# sourceMappingURL=types.d.ts.map